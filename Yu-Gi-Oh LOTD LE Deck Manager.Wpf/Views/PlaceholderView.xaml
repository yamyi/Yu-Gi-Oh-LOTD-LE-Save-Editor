<UserControl x:Class="YuGiOhDeckManager.Wpf.Views.PlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">
    <Grid >
        <Border Style="{StaticResource DuelLinksPanelStyle}"
                Width="360" Height="160"
                HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="{Binding PageTitle, ElementName=Root}"
                           FontSize="20" FontWeight="Bold"
                           Foreground="{StaticResource TextAccentBrush}"
                           HorizontalAlignment="Center" />
                <TextBlock Text="Coming up next in the rewrite"
                           FontSize="12" Foreground="{StaticResource TextMutedBrush}"
                           Margin="0,8,0,0" HorizontalAlignment="Center" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
